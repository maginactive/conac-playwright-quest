import { test, expect } from '@playwright/test';

test('Search for "money forward" on Google, tab, and click on the first search result', async ({ page }) => {
  // Step 1: Navigate to Google
  await page.goto('https://www.google.com/?hl=en');

  // Step 2: Type the search query into the search box
  

  // Step 3: Press Tab to move to the next element
  

  // Step 4: Click the "Google Search" button
  

  // Step 5: Wait for the search results to load 5000ms
  

  // Step 6: Verify the page title contains the search term
  
});